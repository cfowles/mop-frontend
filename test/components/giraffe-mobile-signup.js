// import React from 'react'
// import { expect } from 'chai'

// import { createMockStore } from 'redux-test-utils'

// import { mount, shallow } from 'enzyme'
// import { unwrapReduxComponent } from '../lib'

// import SignatureAddForm from '../../src/containers/signature-add-form'
// import MobileSubscribe from '../../src/components/theme-giraffe/signature-mobile-subscribe'

describe('<MobileSubscribe />', () => {
  // TODO write tests --> mobile opt in displaying, not displaying
  it('renders <MobileSubscribe /> and displays fields', () => {
    // const mobileSignUp = shallow(<MobileSubscribe />)
    //
    // expect(mobileSignUp.find('input[name="phone"]').length).to.equal(1)
    // expect(mobileSignUp.find('input[name="mobile_optin"]').length).to.equal(0)
  })
})
